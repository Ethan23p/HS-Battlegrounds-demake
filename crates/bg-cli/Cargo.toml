[package]
name = "bg-cli"
description = "Fixture generator: resolves an Action Phase and emits it as JSON for a front end"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "bg"
path = "src/main.rs"

[dependencies]
bg-sim = { workspace = true }
serde_json = { workspace = true }
