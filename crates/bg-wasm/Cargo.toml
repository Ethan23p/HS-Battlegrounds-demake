[package]
name = "bg-wasm"
description = "Shallow WebAssembly frontend seam for bg-sim: JSON in, resolved Action Phase out"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bg-sim = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
